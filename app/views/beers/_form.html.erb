<%= form_with(model: beer) do |form| %>
  <% if beer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
        <% beer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for(@beer) do |form| %>
  <div>
    name: <%= form.text_field :name %>
    <%= form.select :style_id, options_from_collection_for_select(@styles, :id, :to_s ) %>
    <%= form.select :brewery_id, options_from_collection_for_select(@breweries, :id, :to_s) %>

    <%= form.submit %>
    <% end %>
  </div>
<% end %>